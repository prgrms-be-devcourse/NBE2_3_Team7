version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/home/hunmin
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: deploy-user.sh
      timeout: 60
      runas: root
